import { PlayerContainer } from '@components/PlayerCard/components/PlayerContainer';
import { PlayerNickname } from '@components/PlayerCard/components/PlayerNickname';
import { PlayerImage } from '@components/PlayerCard/components/PlayerImage';
import { PlayerName } from '@components/PlayerCard/components/PlayerName';

export const Player = {
  Container: PlayerContainer,
  Nickname: PlayerNickname,
  Name: PlayerName,
  Image: PlayerImage,
};
